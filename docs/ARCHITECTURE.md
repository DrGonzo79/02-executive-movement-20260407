# Architecture

## Frontend
React + TypeScript + Vite interactive demo pipeline.

## Backend
FastAPI service with modular scraping/parser simulation in `backend/engine`.

## Static site
The `site/` folder contains the stronger polished HTML opportunity page.

## Deployment
GitHub Actions deploys the frontend build to GitHub Pages.
